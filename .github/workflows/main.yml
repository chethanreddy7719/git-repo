name: git_repo

on: 
  schedule: 
    - cron: "*/15 * * * *"

jobs:
  demo:
    runs-on: ubuntu-latest

    steps:
    - name: Print Message
     run: echo "${{ github.actor }} schedule triggered every 15 minutes"
    
