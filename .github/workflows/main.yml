name: git_repo

on:
     push:
       branches:
         - master 

jobs:
  demo:
    runs-on: ubuntu-latest

    steps:
      - name: checkout
        uses: actions/checkout@v3
        with:
          fetch-depth: 0
      - name: runned by
        run: echo "${{ github.actor }} The checkout is completed

        
    
